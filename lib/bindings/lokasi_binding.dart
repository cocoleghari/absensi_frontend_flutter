import 'package:flutter/material.dart';

class LokasiBinding extends StatelessWidget {
  const LokasiBinding({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
