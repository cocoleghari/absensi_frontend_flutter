import 'package:flutter/material.dart';

class PusatLokasiBinding extends StatelessWidget {
  const PusatLokasiBinding({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
