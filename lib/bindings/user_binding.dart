import 'package:flutter/material.dart';

class UserBinding extends StatelessWidget {
  const UserBinding({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
