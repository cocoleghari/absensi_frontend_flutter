import 'package:flutter/material.dart';

class AkunTableWidget extends StatelessWidget {
  const AkunTableWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
