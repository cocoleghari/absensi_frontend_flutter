import 'package:flutter/material.dart';

class AkunHeaderWidget extends StatelessWidget {
  const AkunHeaderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
