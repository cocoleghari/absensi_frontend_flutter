import 'package:flutter/material.dart';

class PusatLokasi extends StatelessWidget {
  const PusatLokasi({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
