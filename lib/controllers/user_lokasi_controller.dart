import 'package:flutter/material.dart';

class UserLokasiController extends StatelessWidget {
  const UserLokasiController({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
