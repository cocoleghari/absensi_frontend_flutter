import 'package:flutter/material.dart';

class LokasiController extends StatelessWidget {
  const LokasiController({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
