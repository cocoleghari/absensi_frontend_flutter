import 'package:flutter/material.dart';

class AdminAbsensiController extends StatelessWidget {
  const AdminAbsensiController({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
